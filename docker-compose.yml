version: "3.8"

services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: postgresql
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: calculadora
    ports:
      - "5432:5432"